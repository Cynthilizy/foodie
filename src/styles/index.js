import { colors } from "./colors.styles";
import { space, lineHeights } from "./spacing.styles";
import { sizes } from "./sizes.styles";
import { fonts, fontWeights, fontSizes } from "./fonts.styles";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
